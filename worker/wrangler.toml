name = "ramacciato-sconti"
main = "worker.js"
compatibility_date = "2024-11-01"

# I secret NON vanno qui. Impostali con:
#   npx wrangler secret put PAYPAL_CLIENT_ID
#   npx wrangler secret put PAYPAL_SECRET
# Variabile in chiaro (facoltativa): live | sandbox
[vars]
PAYPAL_ENV = "live"
